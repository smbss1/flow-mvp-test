---
id: T02
slice: S01
milestone: M001
title: Add capitalize helper with test
domain: backend
expertise: ["js"]
estimate: S
depends_on: []
files_touched:
  - "src/capitalize.js"
  - "tests/capitalize.test.js"
must_haves:
  truths:
    - "capitalize converts 'hello' to 'Hello'"
    - "capitalize keeps already-capitalized words unchanged"
    - "Test file uses node:test"
  artifacts:
    - "src/capitalize.js"
    - "tests/capitalize.test.js"
  key_links: []
acceptance_criteria:
  - "src/capitalize.js exports capitalize(input)"
  - "tests/capitalize.test.js has at least 3 cases"
  - "npm test passes via node --test"
out_of_scope:
  - "package.json"
risks: []
addresses_decisions:
  - "D-01"
---

Implement `capitalize(input: string): string` that:
- uppercases the first character
- leaves the rest unchanged
- handles empty string by returning empty string
