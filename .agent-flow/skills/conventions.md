<!--
Coding conventions for this project.

Examples:
- Naming: camelCase / snake_case / PascalCase per element type
- File organization, module boundaries
- Error handling style
- Forbidden patterns (e.g. no `any` in TypeScript, no `unwrap()` in Rust)

Remove these comments and write your own conventions below this line.
-->
